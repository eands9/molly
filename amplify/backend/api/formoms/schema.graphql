type calEvent @model @auth(rules: [
  { allow: owner },
  { allow: private, operations: [read], mutations: [create, update, delete] }
  ])
  {
    id: ID!
    name: String!
    lname: String
    start: String
    end: String
    cycle_length: Int
    duration: Int
  }